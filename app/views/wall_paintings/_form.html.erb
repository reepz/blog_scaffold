<%= form_for(@wall_painting) do |f| %>
  <% if @wall_painting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wall_painting.errors.count, "error") %> prohibited this wall_painting from being saved:</h2>

      <ul>
      <% @wall_painting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
