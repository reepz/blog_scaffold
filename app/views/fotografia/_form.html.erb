<%= form_for(@fotografium) do |f| %>
  <% if @fotografium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fotografium.errors.count, "error") %> prohibited this fotografium from being saved:</h2>

      <ul>
      <% @fotografium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
